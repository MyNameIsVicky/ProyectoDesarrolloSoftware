@model ProyectoDesarrolloSoftware.Models.ModuloMedicina.Medicamento

@{
    ViewData["Title"] = "Nuevo Medicamento";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="text-center mb-4">
                1
                <h2 class="text-primary fw-bold display-5">Registrar Medicamento</h2>
                <p class="text-muted lead">Centro Médico Carlos Monge Alfaro</p>
                <hr class="w-25 mx-auto text-primary" />
            </div>

            <div class="card border-0 shadow-sm rounded-3">
                <div class="card-body p-4">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger border-0 small py-2 mb-4 rounded-2"></div>

                        <div class="form-group mb-4">
                            <label asp-for="NombreMedicamento" class="form-label small fw-bold">Nombre del Medicamento</label>
                            <input asp-for="NombreMedicamento" class="form-control" placeholder="Ej: Paracetamol 500mg" />
                            <span asp-validation-for="NombreMedicamento" class="text-danger small d-block mt-1"></span>
                        </div>

                        <div class="d-flex flex-column flex-md-row gap-2 mt-4">
                            <button type="submit" class="btn btn-outline-primary py-2 fw-bold flex-fill rounded-2">Registrar Medicamento</button>
                            <a asp-action="Index" class="btn btn-outline-secondary py-2 fw-bold flex-fill rounded-2">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
